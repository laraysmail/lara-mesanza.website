<section class="advisory" id="asesoramiento-teaser">
  <div class="wrap reveal">
    <div class="advisory-grid">
      <div class="advisory-visual"><span class="badge">Asesoramiento</span></div>
      <div class="advisory-body">
        <span class="kicker" style="color:var(--pop);">// para cuando quieres ayuda de verdad</span>
        <h2>Diagrama eléctrico, diseñado para ti</h2>
        <p style="margin-top:16px; color:var(--mist); opacity:.85; font-size:.98rem; line-height:1.6; max-width:440px;">Smaïl diseña tu sistema eléctrico a medida. Tu furgo, tu presupuesto, tu consumo real.</p>
        <div class="price">Desde 200 €</div>
        <a class="cta-btn" href="/asesoramiento">Ver el servicio completo →</a>
      </div>
    </div>
  </div>
</section>

<style>
  .advisory { padding: 100px 0; background: var(--forest); color: var(--cream); position: relative; overflow: hidden; }
  .advisory-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 44px; align-items: center; position: relative; z-index: 1; }
  @media (max-width: 860px) { .advisory-grid { grid-template-columns: 1fr; } }
  .advisory-visual {
    aspect-ratio: 4/3; border-radius: 22px; position: relative; overflow: hidden;
    background: radial-gradient(circle at 75% 20%, rgba(193,96,47,0.28), transparent 55%), linear-gradient(150deg, var(--moss), var(--pine));
  }
  .advisory-visual .badge {
    position: absolute; top: 18px; left: 18px; font-family: var(--mono); font-size: 10.5px; text-transform: uppercase;
    background: rgba(16,25,21,0.55); color: var(--pop); padding: 6px 14px; border-radius: 100px; border: 1px solid rgba(193,96,47,0.35);
  }
  .advisory-body .price { font-family: var(--mono); color: var(--sage); font-size: .95rem; margin-top: 14px; }
  .advisory-body a.cta-btn {
    margin-top: 22px; display: inline-flex; align-items: center; gap: 8px; background: var(--pop); color: var(--pine);
    font-family: var(--display); font-weight: 600; font-size: 13.5px; padding: 14px 26px; border-radius: 100px;
    transition: background .25s ease;
  }
  .advisory-body a.cta-btn:hover { background: #d97442; }
</style>
