---
import BaseLayout from '../layouts/BaseLayout.astro';
import VideoCard from '../components/VideoCard.astro';
import FilterPills from '../components/FilterPills.astro';
import site from '../data/site.json';
import videosFallback from '../data/videos.json';
import socialVideos from '../data/social-videos.json';
import { fetchLatestVideos } from '../lib/youtube.js';

const youtubeVideos = (await fetchLatestVideos(site.youtubeChannelId, 8, videosFallback))
  .map((v) => ({ ...v, platform: 'youtube' }));

const featured = youtubeVideos[0];
const rest = [...youtubeVideos.slice(1), ...socialVideos];

const channelUrl = site.youtubeChannelId ? `https://www.youtube.com/channel/${site.youtubeChannelId}` : '#';
const xDirs = ['-40px', '40px', '-40px'];
---

<BaseLayout title="Vídeos — Lara Mesanza" description="Camperización, viajes low-cost y reformas — YouTube, Instagram y TikTok en un solo sitio.">
  <section class="videos-head">
    <div class="wrap reveal">
      <span class="kicker">// consejos buenos, bonitos y baratos</span>
      <h1>Vídeos</h1>
      <p>Todo lo que subo: YouTube, Reels y TikToks, cada uno con lo suyo.</p>
      <a class="channel-link" href={channelUrl} target={channelUrl === '#' ? undefined : '_blank'} rel={channelUrl === '#' ? undefined : 'noopener noreferrer'}>
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M22 12c0-1.5-.15-2.6-.5-3.5a3 3 0 00-2.1-2.1C18 6 12 6 12 6s-6 0-7.4.4A3 3 0 002.5 8.5C2.15 9.4 2 10.5 2 12s.15 2.6.5 3.5a3 3 0 002.1 2.1C6 18 12 18 12 18s6 0 7.4-.4a3 3 0 002.1-2.1c.35-.9.5-2 .5-3.5z"/><path d="M10 9.5l5 2.5-5 2.5v-5z" fill="var(--cream)"/></svg>
        Ver el canal completo en YouTube →
      </a>
      <FilterPills
        targetId="videos-grid"
        options={[
          { value: 'todo', label: 'Todo' },
          { value: 'youtube', label: 'YouTube' },
          { value: 'instagram', label: 'Instagram' },
          { value: 'tiktok', label: 'TikTok' },
        ]}
      />
    </div>
  </section>

  <section class="videos-body">
    <div class="wrap">
      <div class="video-grid" id="videos-grid">
        {featured && <VideoCard category={featured.category} title={featured.title} url={featured.url} thumbnail={featured.thumbnail} platform="youtube" featured />}
        {rest.map((v, i) => (
          <VideoCard category={v.category} title={v.title} url={v.url} thumbnail={v.thumbnail} platform={v.platform} x={xDirs[i % 3]} d={`${(i % 3) * 0.08}s`} />
        ))}
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .videos-head { padding: 160px 0 40px; }
  .videos-head h1 {
    font-family: var(--display); font-weight: 700; letter-spacing: -.02em;
    font-size: clamp(2.2rem, 5vw, 3.4rem); line-height: 1.05;
  }
  .videos-head p { margin-top: 20px; max-width: 480px; opacity: .7; font-size: .98rem; line-height: 1.55; }
  .channel-link {
    margin-top: 24px; display: inline-flex; align-items: center; gap: 10px; background: var(--forest); color: var(--cream);
    font-family: var(--display); font-weight: 600; font-size: .88rem; padding: 12px 22px; border-radius: 100px;
    transition: background .25s ease;
  }
  .channel-link svg { width: 18px; height: 18px; }
  .channel-link:hover { background: var(--moss); }

  .videos-body { padding: 40px 0 130px; }
  .video-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 22px; align-items: start; }
  @media (max-width: 900px) { .video-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 600px) { .video-grid { grid-template-columns: 1fr; } }
</style>
